<%- include("partials/header") -%>

<div class="main-cont">

    <h1>Hello</h1>
    <p><%= homeContent %></p>

    <% postList.forEach(function (element) { %>
        <% const truncatedContent = element.content.substring(0, 150) %>
        <h1> <%= element.title %></h1>
        <p><%= truncatedContent %>
            <% if(element.content.length > 150){ %>
                ...
            <% }else{ %>

            <% } %>
            <span><a class="read-more-text" href="/posts/<%= element._id %>">Read More</a></span>
        </p>

    <% }) %>
</div>

<%- include("partials/footer") -%>